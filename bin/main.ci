// Arithmetic
a = 1;
b = 2;
c = a + b;

// Conditional jumping to implement a for loop
i = 0;
mark begin_loop:
goto end_loop if i <= c;

print_line(i);
i = i + 1;
goto begin_loop;

mark end_loop:

return 0;
